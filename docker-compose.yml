services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: radar_leiloes
      POSTGRES_USER: radar
      POSTGRES_PASSWORD: radar
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
